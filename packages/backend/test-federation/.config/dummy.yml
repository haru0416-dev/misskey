configVersion: 2

instance:
  url: https://example.com

database:
  primary:
    host: localhost
    name: misskey
    user: postgres
    password:
      plainText: postgres

valkey:
  connections:
    primary:
      host: localhost
