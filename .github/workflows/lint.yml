name: Lint

on:
  push:
    branches:
      - master
      - develop
    paths:
      - packages/aiscript/**
      - packages/mfm-js/**
      - packages/backend/**
      - packages/frontend/**
      - packages/frontend-embed/**
      - packages/frontend-shared/**
      - packages/i18n/**
      - packages/icons-subsetter/**
      - packages/sw/**
      - packages/misskey-js/**
      - .devcontainer/**
      - .github/ISSUE_TEMPLATE/**
      - .github/scripts/**
      - .github/workflows/**
      - .github/*.yml
      - .oxfmtrc.json
      - .oxlintrc.json
      - '*.json'
      - '*.json5'
      - '*.toml'
      - '*.yaml'
      - '*.yml'
      - bun.lock
      - package.json
      - scripts/**
      - tests/e2e/**
  pull_request:
    paths:
      - packages/aiscript/**
      - packages/mfm-js/**
      - packages/backend/**
      - packages/frontend/**
      - packages/frontend-embed/**
      - packages/frontend-shared/**
      - packages/i18n/**
      - packages/icons-subsetter/**
      - packages/sw/**
      - packages/misskey-js/**
      - .devcontainer/**
      - .github/ISSUE_TEMPLATE/**
      - .github/scripts/**
      - .github/workflows/**
      - .github/*.yml
      - .oxfmtrc.json
      - .oxlintrc.json
      - '*.json'
      - '*.json5'
      - '*.toml'
      - '*.yaml'
      - '*.yml'
      - bun.lock
      - package.json
      - scripts/**
      - tests/e2e/**
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  oxlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          submodules: true
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version-file: .bun-version
      - name: Cache bun install cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - name: Build slacc
        # リポジトリ内でビルドするネイティブモジュール。backend の実行に必要。
        run: bun run --filter slacc build
      - run: bun run lint:ox

  oxfmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          submodules: true
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version-file: .bun-version
      - name: Cache bun install cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - name: Build slacc
        # リポジトリ内でビルドするネイティブモジュール。backend の実行に必要。
        run: bun run --filter slacc build
      - run: bun run format:ox:check

  vue-templates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          submodules: true
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version-file: .bun-version
      - name: Cache bun install cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - name: Build slacc
        # リポジトリ内でビルドするネイティブモジュール。backend の実行に必要。
        run: bun run --filter slacc build
      - run: bun run lint:vue-templates

  typecheck:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        workspace:
          - '@syuilo/aiscript'
          - mfm-js
          - backend
          - frontend
          - frontend-embed
          - i18n
          - icons-subsetter
          - sw
          - misskey-js
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          submodules: true
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version-file: .bun-version
      - name: Cache bun install cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - name: Build slacc
        # リポジトリ内でビルドするネイティブモジュール。backend の実行に必要。
        run: bun run --filter slacc build
      - run: bun run build:frontend-deps
      - run: bun run --bun --filter ${{ matrix.workspace }} typecheck
      - if: matrix.workspace == 'frontend'
        run: bun run typecheck:playwright
