## Unreleased

### Note

**このバージョンでは動作要件が変更されます。アップグレード前に環境を確認してください。**

- センシティブメディアの判定 (NSFW検出) が、本体に内蔵された nsfwjs による推論から、外部サービス [sensitive-detector](https://github.com/misskey-dev/sensitive-detector) への HTTP 呼び出し方式に変更されました。
	- これに伴い、本体から `nsfwjs` / `@tensorflow/tfjs` / `@tensorflow/tfjs-node` および同梱の NSFW 判定モデルが削除され、インストール要件 (ネイティブ ML スタック) が緩和されました。
	- **センシティブ判定機能を利用しているサーバーは対応が必要です。** 別途 [sensitive-detector](https://github.com/misskey-dev/sensitive-detector) サービスを立ち上げ、コントロールパネルの「モデレーション > センシティブなメディアの検出」で接続先 URL を設定してください。接続先が未設定の場合、センシティブ判定は行われません (すべて非センシティブ扱い)。
	- 画像の正規化・動画フレームの抽出・しきい値判定・集約は引き続き本体側で行われ、外部サービスには正規化済み画像の推論のみを委譲します。
- Bun 1.4.0 以降が必須です。
	- Node.js / pnpm ベースの実行環境から Bun ベースの実行環境へ移行しました。
	- Docker Image は `oven/bun:1.4.0` 系です。
- バックエンドで画像処理に用いているライブラリ sharp のシステム要件により、**SSE4.2 命令セットをサポートしていない x86_64 CPU では動作しません**。仮想マシンへデプロイしている場合や古いハードウェアを使っている場合は、アップグレード前に環境を確認してください。ARM64 など x86_64 ではない環境では影響ありません。
- 2025.4.0 以前の設定情報の移行処理が削除されました
	- 2025.4.0 以前から直接アップデートする場合、クライアント設定は移行されません。移行したい場合は一度 Misskey 2026.5.1 を経由してください。

### General

- Change: バージョン番号を upstream の日付形式 (2026.6.x) から切り離し、`0.1.0` から振り直し (nodeinfo の `software.version` と「サーバー情報」の表示が変わる)
- Enhance: YAML処理の直接依存を整理
- Fix: 依存パッケージの既知脆弱性39件中38件を修正 (hono 4.12.34 / undici 7.29.0 / postcss 8.5.23 / find-my-way 9.7.0 ほか。親パッケージが古い版を固定していたものは overrides で修正版を強制。残1件は esbuild dev server の脆弱性で、当リポジトリに該当経路なし)
- Fix: AiScriptのタイマー・文字列index処理と、misskey-jsのストリーミング再接続・入力検証を修正
- Enhance: プロジェクトの表示名とNodeInfoのソフトウェア名をErebiaへ変更
- Feat: 条件に一致したURLプレビューのサムネイルをセンシティブ扱いにできるように
- Fix: 非公開ノートのリアクション・差分取得とアンケート投票のアクセス制御を修正
- Fix: AiScriptとストリーミング接続の無制限なメモリ消費を防止
- Fix: 指定公開ノートのメンション配送と、非公開の親ノートを持つ指定公開返信の連合を修正
- Enhance: エラー・トレース収集をSentryからOpenTelemetry OTLPへ移行
- Enhance: ノート翻訳サービスとしてセルフホスト可能なLibreTranslateに対応
- Enhance: 非推奨だった `read:messaging` / `write:messaging` API 権限スコープを削除
- Enhance: ゲーム機能を削除
- Enhance: 未使用・重複依存を削除し、パスワード処理をBun標準APIへ置換 (埋め込みウィジェットのuuidをネイティブ `crypto.randomUUID()` に置換、`form-data` 経由の High 脆弱性1件を解消)
- Enhance: メンション抽出処理を共通化し、同一アカウントへの重複した検索や処理を抑制
- Fix: AiScript / AiSON のオブジェクト変換で `__proto__` キーがプロトタイプとして解釈される問題を修正
- Fix: AiScriptの循環参照を含む値のJavaScript変換・文字列化でスタックオーバーフローや誤った省略表示が発生する問題を修正
- Fix: AiSONで記号・空白・非ASCII文字を含むオブジェクトキーを文字列化すると再解析できない問題を修正
- Fix: AiSONが非有限数から再解析不能な文字列を生成する問題と、負のゼロの符号を失う問題を修正
- Fix: AiSONとAiScriptのメタデータ・属性で負数リテラルを解析できない問題を修正
- Fix: AiScriptのerror値をJavaScript変換・文字列化すると内部エラーまたは不明な表示になる問題を修正
- Fix: AiScriptの配列値を通常表示すると `arr<null>` と表示される問題を修正
- Fix: JavaScriptの疎配列をAiScript / AiSONへ変換すると欠損要素が残り、表示エラーや再解析不能な配列リテラルが発生する問題を修正
- Fix: AiSONのインデント指定が無制限に適用され、`Infinity`で文字列化に失敗する問題を修正
- Fix: AiScript値の文字列表現で改行・タブ・引用符やオブジェクトキーが正しくエスケープされず、表示が崩れる問題を修正
- Fix: AiScriptの数値リテラルが範囲外の場合に暗黙に無限大へ変換される問題を修正
- Fix: AiScriptの数値リテラルで不正な位置の区切り文字が受理される問題を修正
- Fix: AiScriptの`num.to_hex()`が非有限数に16進数ではない文字列を返す問題を修正
- Fix: AiScriptの関数値で引数の型情報が失われ、ネストしたジェネリック型を参照できない問題を修正
- Fix: AiScriptの未知の複合型に対する構文エラーで型名が正しく表示されない問題を修正
- Fix: misskey-jsのストリーミング接続を複数回disposeすると共有接続が解放されなくなる問題を修正
- Enhance: ストリーミングのRedisイベント振り分けを接続数に比例しないよう最適化
- Fix: misskey-jsと埋め込みウィジェットのAPIエラー判定・必須リクエスト型を修正
- Feat: コントロールパネルから二要素認証を解除できるように

### Client

- Enhance: 画像アップロード時の縮小品質を改善 (`@misskey-dev/browser-image-resizer` への依存をやめ、ブラウザ組み込みのリサンプラを使う実装へ置き換え。実測 PSNR で 42.5dB → 47.2dB (Chromium) / 42.5dB → 47.6dB (Firefox))
- Fix: ジョブキューの管理画面から、実体の無くなった「Paused」状態での絞り込みを削除 (BullMQ v6 でキューの一時停止はキュー単位の状態になり、個別ジョブが paused として並ばなくなったため)
- Fix: 画像変換・アップロード、認証URL、ナビゲーション、Service Workerの複数アカウントと言語切替を修正
- Enhance: アンテナの受信ソースに「フォローしているユーザーのノート」と「指定したリストのユーザーのノート」を選べるように (サーバー側の判定を修正したため復帰)
- Fix: 仮想化されたタイムラインをスクロールすると、全てのノートのユーザーアイコンが同時に追従してしまう問題を修正
- Feat: ジョブキューの管理画面にデッドレター (処理を諦めたジョブ) の一覧・再試行・破棄を追加 (アカウントの完全削除などが止まったままにならないように)
- Fix: QRコードリーダーで、他のページから戻ってきた際にカメラが再開されず、離脱時も停止しない問題を修正
- Fix: 画像の切り抜きダイアログで、切り抜きの初期化やエンコードに失敗した場合に無反応になっていた問題を修正 (エラーを通知するように)
- Fix: ドライブのアップロード失敗時やファイル未選択時のエラーに理由が含まれない問題を修正
- Enhance: Erebiaブランドデザインを導入 (新アイコン・ワードマーク、アイリスバイオレット基調の新デフォルトテーマ「Erebia Dark」「Erebia Light」、角丸を抑えたシャープなUI、マニフェスト・ブート画面・エラー画面・メール配色の刷新)
- Enhance: テーマの色指定でoklch()表記が使えるように (Erebiaテーマのパレットはoklchで定義)
- Enhance: ノートUIの余白・整列・フッター配置・引用枠スタイルを調整 (引用ブロックの内側余白喪失バグの修正、リノート行と本文の左端整列、モバイルでのタップターゲット44px化を含む)
- Enhance: サイドバーUIを調整 (サーバーアイコンの列整列、項目ピッチ最適化、フル/アイコン表示間のサイズ統一、safe-area対応、フォーカスリング統一、アクティブ項目のコントラスト改善、ラベル表示中の冗長ツールチップ抑止)
- Enhance: サイドバー横の羽形サブボタンを廃止し、折りたたみ/展開とナビバー編集をサイドバー上部のアイコンボタンへ移動
- Enhance: ユーザーページを調整 (バナー未設定時のブランドグラデーション、バナー上テキストの可読性向上、情報テーブルの列整列、統計行の階層とタップ領域、モバイルの名前表示、ページヘッダーのタイトル圧壊防止とタブのアイコン化・アクティブタブ自動スクロール)
- Fix: ユーザーページで画面リサイズ時にサイドカラムのレイアウトが追従しない問題を修正
- Enhance: 設定画面を調整 (提案バナーの全幅化と逐次表示、モバイルでの戻り導線追加、ナビのhover表現統一、モバイル設定トップのタイル角形化、旧ブランド色のヒーローバナー置換、無効セクションの斜線ハッチ削除、リンク行とアコーディオン行の規格統一)
- Enhance: 初回セットアップ画面を調整 (サーバー初回セットアップ画面をブランドワードマーク化しパネル影・角丸・余白をトークン化、波背景アニメーションのアニメーション設定連動と装飾要素のスクリーンリーダー除外・入力欄のオートコンプリート指定、サーバー推奨設定サマリを1行の定義リストへ圧縮、オンボーディングウィザードのモーション削減設定対応・ステップ遷移時のフォーカス復帰・低い画面高での下部ボタン見切れ防止・フォロー候補カードのプレースホルダ配色のテーマ追従・完了画面のボタン階層整理)
- Enhance: チャット/DMのメッセージ画面を調整 (メッセージバブル・リアクション・日付区切り・新着インジケータ・入力フォーム・履歴リストの余白と角丸をデザイントークンへ統一、自分のリアクションを右端整列しアクセント面で選択状態を表示、リアクションをキーボード操作可能にしaria-pressedで状態を通知、アイコンのみボタン・メッセージ入力欄・添付ファイルにアクセシブルネームを付与、メッセージ列をログ領域化し新着メッセージをライブリージョンで読み上げ、装飾アイコンのスクリーンリーダー除外、デッド・空CSSの除去)
- Enhance: チャットの会話表示を改善 (メッセージバブルに最大幅を設定し長文でも発言者の左右が判別しやすく、同一送信者の連続メッセージはアバター・送信者名を省略して行間を詰めたグループ表示に、自分のアバターは画面幅を問わず非表示に統一)
- Enhance: 吹き出しをドッキングコーナー形状に刷新 (テール装飾を廃止し、発言者側の上角だけを鋭角にして向きを示すLinear風の精密な形に。チャットのバブルとユーザーページのフォロワー向けメッセージに適用)
- Fix: タイムラインのロード直後に全投稿が一瞬同じ位置に重なって表示されてから展開される問題を修正 (仮想スクロールの行計測を同期化し、レイアウト確定前の行が重なったフレームは描画前検査で不可視化。ノート仮想行のプレースホルダ誤計測と、初回表示時の非仮想→仮想の二重マウントも解消)
- Enhance: 通知・チャンネル・クリップ・ギャラリー・Pages・Play・お知らせ・みつけるの各画面をデザイントークンへ統一 (通知行のジオメトリをノートと揃え混在フィードの列ズレを解消、通知バッジ7色のテーマ追従化、チャンネルバナーのフォールバック配色統一とアクションボタンの右上集約、ギャラリーのhoverズーム廃止とタイトル2行clamp、Pages/Playカードの未定義CSS変数解消、Playカードの非公開バッジ追加、お知らせの未読ドット化・既読ミュート・バナーアイコンのアクセント地での白色統一、クリップの更新日時を相対表示化、タグクラウドのflexレイアウト化)
- Fix: 通知のスクロール追従アバターがページヘッダーの裏に隠れる問題と、ギャラリーカードにキーボードで到達できない問題を修正 (各画面のアイコンのみボタンへのアクセシブルネーム付与、装飾アイコンのスクリーンリーダー除外、画像のalt付与を含む)
- Enhance: ウィジェットをデザイントークンへ統一 (編集モードのオーバーレイボタンのテーマ追従化と設定/削除ボタンのアクセシブルネーム付与、編集ヘッダーと切替ボタンのレイアウト統一、プロフィールウィジェットのバナーフォールバック、RSSティッカー・マーキーのモーション削減設定対応、RSS取得のエラーハンドリング、サーバーメトリクスの表示切替ボタンのアクセシブルネーム付与とチャート色トークン化、時計・カレンダー・アクティビティ等の余白トークン化、ユーザーリストの空状態表示)
- Fix: 通知ウィジェットやデッキの極狭カラムに埋め込まれたノートで、アクションボタン (返信・リノート・リアクション・メニュー) が2段に折り返す問題を修正 (スマートフォン幅のタップターゲット44pxは維持しつつ、300px以下の狭いカラムでのみ寸法を戻して1行に収める)
- Enhance: ポップアップ・オーバーレイ系 (モーダル/ダイアログ/メニュー/トースト/ツールチップ) をデザイントークンとa11yパターンへ統一 (モーダルウィンドウ・フローティングウィンドウにrole=dialog/aria-modalと閉じるボタンのアクセシブルネーム付与、ダイアログのrole=dialog/alertdialog分岐とタイトル・本文のaria関連付け、トースト・待機ダイアログのrole=status化、メニュー項目・ランチパッド・ツールチップ・スポットライトの余白/角丸/影/durationのトークン化、装飾アイコンのスクリーンリーダー除外、即時開閉時のモーダル配置計算のnullガード)
- Fix: ラジオボタン・スライダーがキーボードで操作できず、スクリーンリーダーに状態が伝わらない問題を修正 (ダークテーマでスライダー・容量メーターの溝が見えない問題も修正)
- Fix: 折りたたみセクション (アコーディオン) の開閉アニメーション中、入れ子になった開いたままのアコーディオンのヘッダーが下方向へずれて終了時に戻る問題を修正
- Fix: 折りたたみセクションを開く際に、開いた状態の内容が一瞬全高で見えてから開き直すことがある問題と、閉じアニメーション中に再度開くと高さがずれる問題を修正
- Enhance: 「Erebiaについて」を追加し、「Misskeyについて」と分離
- Fix: 「Misskeyについて」の名称と内容を上流版へ復元
- Enhance: 画像と動画を混在して閲覧できるMisskeyネイティブのメディアlightboxを追加
- Fix: デバイスタイプをスマートフォンに固定している状態で画面幅が広いとき、画面左上のアイコンが表示されない問題を修正
- Fix: 自分宛てメンションの判定、チャット入力中のIME確定操作、管理画面の秘密値へのパスワード誤補完を修正
- Fix: 通知を1件閉じた際に全通知が既読になる問題、複数タブ間の設定保存競合、極端な縦横比の画像による過大なBlurhash描画を修正
- Fix: プラグインがクライアント起動を妨げる問題、認証callback URL、ミュートノートのキーボード操作を修正
- Fix: Service WorkerがMisskey以外のCache Storageを削除する問題を修正
- Enhance: コントロールパネルのダッシュボードを見やすく整理
- Enhance: コードハイライト・URL抽出・文字列検索の重複処理を抑制
- Enhance: 物理演算・WebGLエフェクト・タイムライン整形の処理とリソース管理を最適化
- Enhance: 軽量ディレクティブのイベント解放とデッキ編集時の複製処理を最適化
- Enhance: 設定統合と画像編集のGPU処理・リソース管理を最適化
- Enhance: MFM・グリッド・メディア判定・共通UIの再描画とライフサイクル処理を最適化
- Enhance: 絵文字オートコンプリート検索時のDB走査と並べ替えを最適化
- Enhance: ツールチップ、クリックアニメーション、画面維持処理のリソース管理を最適化
- Enhance: 音声の重複読み込みを抑制し、メタデータ取得処理をイベント駆動化
- Enhance: 絵文字・Query・プラグインのリアルタイム更新処理を線形化・キャッシュ
- Enhance: ノートのミュートワード判定で設定の前処理と正規表現を再利用
- Enhance: フォーム、オートコンプリート、スクロール位置復元の監視・イベント処理を最適化
- Enhance: アプリシェル、ナビゲーション、タイムラインのデザインとモバイル操作性を改善
- Enhance: プロフィール設定から自分のアイコン画像・バナー画像を削除できるように
- Enhance: 右横書き・双方向テキストを含むノート本文の表示を改善
- Enhance: MFMの検索フォームで使用する検索エンジンを選べるように
- Enhance: ノート詳細画面で親ノートを折りたためるように
- Enhance: 管理画面から新規登録のレート制限を調整できるように
- Enhance: タイムラインとデッキで画像・動画をグリッド表示するメディアタイムラインを追加
- Enhance: 通知を個別に非表示にできるように
- Enhance: ノート検索で添付ファイル、センシティブファイル、返信、引用、CW、公開範囲による絞り込みに対応
- Enhance: Paginator のカーソル計算・重複排除・リアクティビティを最適化し、重複取得と古いリクエストの競合を抑制
- Enhance: TanStack Query を導入し、主要な参照APIと共有キャッシュの重複取得・無効化・Streaming同期を統合
- Enhance: クライアントの状態・設定管理を Pinia ベースへ移行し、永続化書き込みのバッチ化とアカウント単位のタブ間同期を改善
- Enhance: 旧 `/authorize-follow` と Mastodon 互換の `/authorize_interaction` リダイレクト、未使用の LocalStorage 絵文字キャッシュキー、旧カスタムテーマの `kind` フィールド互換、非推奨の `i18n.t()` API を削除。リモートログイン時は `/lookup?uri=` を使用
- Enhance: タイムライン等の画像をビューポート近傍まで読み込まないように変更 (IntersectionObserver による自前遅延読み込みを導入。あわせて、同一URLの画像がページ内の別要素で既にブラウザキャッシュ済みの場合に `load` イベントが発火せずBlurhashプレースホルダーのまま表示が固まる問題を修正)
- Enhance: 管理画面・アクティビティページのグラフをEChartsベースへ刷新し、要約値・系列切替・詳細表・空データ表示・キーボード操作に対応
- Enhance: Service Worker のバンドルサイズを46%削減 (30.2KB→16.3KB minified)。SWでは使用しないWebSocketストリーミング系コードが `misskey-js` の全体importで混入していたのを、subpath import + type-only import に分離して排除
- Enhance: ストリーミングのメッセージ受信処理を高速化 (misskey-js のチャンネル振り分けを受信毎の線形探索+配列生成から Map による O(1) 参照に変更。接続チャンネル数が多いほど効果大)
- Enhance: ストリーミングの自動再接続処理を内製化し、2020年から未メンテの `reconnecting-websocket` 依存を削除 (指数バックオフ・接続タイムアウト・未接続時の送信バッファリングなど従来挙動を維持、再接続の回帰テストを追加)
- Enhance: iOS Safari のハプティックフィードバック機能 (実験的機能の「ハプティックフィードバックを有効にする」) を削除。使用していた `<input type="checkbox" switch>` トリックが iOS 26.5 で Apple により無効化され、機能しなくなったため
- Fix: 埋め込みウィジェット (frontend-embed) のサーバー提供コンテキスト判定 (`assertServerContext`) で、値が存在するキーだけを見て null 値を見落とすことがあった問題を修正 (通常クライアント側の実装と揃え、値が null でないことも確認するように)
- Enhance: ノートのリアクション表示更新時の並べ替え処理を効率化 (リアクション可否の重複判定と配列の線形探索を削減)
- Enhance: 画像圧縮ライブラリをファイル選択後まで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: ページをウィンドウで開くためのUIを操作時まで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: 画像ギャラリーと動画プレイヤーをメディア表示時まで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: ポップアップメニューとコンテキストメニューを初回操作時まで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: ノート操作メニューの生成処理を操作時まで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: ノートのユーザー・リアクション詳細ツールチップをホバー時まで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: 音声プレイヤーの進捗更新を再生中だけ実行し、停止中や画面離脱後のCPU・バッテリー消費を削減
- Enhance: 動画プレイヤーの進捗更新を再生中だけ実行し、停止中や画面離脱後のCPU・バッテリー消費を削減
- Enhance: 実績データと起動時判定を初回描画後まで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: 初期JavaScriptをmodule preloadし、クライアント起動時のリクエスト待ちを短縮
- Enhance: 数値アニメーションの重複更新を防ぎ、アニメーション無効時と画面離脱後の不要な描画処理を削減
- Enhance: 限定ページ用のWebGL背景を通常起動の初期JavaScriptバンドルから分離し、非表示・アニメーション無効時の描画を停止
- Enhance: 投稿フォームの投票・添付・プレビュー関連UIを必要になるまで遅延読み込みし、初期JavaScriptバンドルを削減
- Enhance: 通知トーストを受信時まで遅延読み込みし、特殊フローで未使用のナビゲーションを初期JavaScriptバンドルから除外
- Enhance: タブ非表示中は相対時刻の共有更新タイマーを停止し、バックグラウンドでのCPU・バッテリー消費を削減
- Enhance: 設定項目メニューの状態反映を100msポーリングからリアクティブ更新へ変更し、メニュー終了時に監視処理を解放
- Enhance: 非表示・バックグラウンド中のツールチップ位置追従を停止し、チャート画面などでの不要な毎フレーム計算を削減
- Enhance: あいちゃんウィジェットのカーソル追従処理をフレーム単位に集約し、高頻度なマウス操作時のレイアウト計算を削減
- Enhance: アクティビティチャートのドラッグ描画をフレーム単位に集約し、操作終了後のイベントリスナーを確実に解放
- Enhance: UNIX時計ウィジェットの更新を画面描画と秒境界に同期し、非表示タブでの更新を停止
- Enhance: 時計コンポーネントの共有描画スケジューラを利用中かつ表示中のみ動作させ、重複する描画予約を防止
- Enhance: ノート更新のフォールバックポーリングを購読中かつ表示中のみ動作させ、通信の重複を防止
- Enhance: タイムライン・RSS・ウィジェット・管理画面などの定期更新を表示中のみ動作させ、非同期通信の重複を防止
- Enhance: 入力欄と選択欄の装飾幅計算を100msポーリングからサイズ変更時のみの更新へ変更
- Enhance: ユーザープレビュー表示中の要素監視を定期ポーリングからライフサイクル連動へ変更
- Enhance: ツールチップ表示元の監視を定期ポーリングからリアクティブなライフサイクル連動へ変更
- Enhance: ミニチャートの再描画を毎秒ポーリングからデータ変更時のみの更新へ変更
- Enhance: 季節の雪・桜エフェクトを非表示タブで停止し、復帰時の時間差を描画から除外
- Enhance: チュートリアルのスポットライト位置計算を毎フレーム実行からレイアウト変更時のみの更新へ変更
- Enhance: アナログ・デジタル時計の通常更新を高頻度描画から秒境界タイマーへ変更
- Enhance: 設定クラウドバックアップを常駐ポーリングから設定保存時の遅延実行へ変更
- Enhance: プル・トゥ・リフレッシュの1msタイマーと入力追従をフレーム単位へ集約
- Fix: グラフの年月フォーマットでdate-fnsの非推奨トークン警告が発生する問題を修正
- Enhance: 長いノート一覧とストリーミングタイムラインを仮想化し、スクロール時のDOM要素数と描画負荷を削減
- Enhance: 入力欄・テキストエリア・選択欄のラベル関連付けとキーボードフォーカス表示を改善
- Fix: 同じ要素のユーザープレビュー対象が変わっても古いユーザーが表示され続ける問題を修正
- Fix: ツールチップをキーボードで利用できない問題と、タッチ操作のキャンセルや入力方式の切り替え後に表示状態が残る・表示されなくなる問題を修正
- Fix: MFM引数から動的styleを生成する際に不正なCSS宣言が混入し得る問題を修正
- Fix: 不正な波括弧を含む翻訳文でクライアントが応答しなくなる問題と、補間引数不足時に`undefined`が表示される問題を修正
- Fix: 不正なテーマデータでクライアントや埋め込みウィジェットの起動・プレビューに失敗する問題を修正し、管理画面での保存時検証を追加
- Fix: 破損したインスタンス情報キャッシュや埋め込みJSONによりクライアント・埋め込みウィジェットが起動できなくなる問題を修正
- Fix: 破損した投稿・チャット下書き、ハッシュタグ履歴、検索候補キャッシュにより入力フォームが動作しなくなる問題を修正
- Fix: テーマ・ナビゲーション切替のView Transitionが失敗すると未処理エラーや切替中の表示状態が残る、または操作が反映されない問題を修正
- Fix: インスタンスクラウドの読込失敗時に内容が空白になる問題と、初期化前の更新でエラーが発生し得る問題を修正
- Fix: 破損したカスタム絵文字キャッシュによりクライアントや埋め込みウィジェットが起動できなくなる問題を修正
- Fix: 破損したアカウント情報・設定プロファイルによりクライアントが起動できなくなる問題を修正し、設定のインポート・クラウド復元時の検証を追加
- Fix: 不正なドラッグデータでドロップ操作が失敗する問題と、対象データ形式が先頭にない場合にドロップできない問題を修正
- Fix: ファイルアップロード時に不正なレスポンスを受信すると処理が完了しない問題を修正
- Enhance: 付箋ウィジェットを個別に保存してタイトルを設定できるようにし、RSSウィジェットのタイトル変更に対応
- Enhance: 投稿フォームの下書き復元方針と、インスタンスティッカーの表示形式を選択できるように
- Enhance: データセーバーやセンシティブメディア表示、UIアニメーションを切り替えるクイック設定を追加
- Fix: 開発環境で更新前の翻訳キャッシュにより画面表示が壊れる問題を修正
- Fix: 初期アカウント設定を閉じた直後の画面遷移で設定画面が再表示されることがある問題を修正
- Feat: 画像ビューアーにピクセルアート拡大モードを追加 (メニューから切り替えると拡大時に補間せず、ドット絵がぼやけない)
- Fix: 画像ビューアーで動画を開くと、動画の上下左右の余白をクリックしても閉じられない問題を修正 (縦横比を確定させ、実際に描画されている範囲だけが動画の当たり判定になるように)
- Fix: 画像ビューアーのスワイプが、動き出しのわずかなぶれで意図しない向きに固定される問題を修正 (向きが確定するまでのしきい値と、短いフリックでの速度判定を追加)
- Fix: RSSウィジェット・RSSティッカーで `javascript:` などのリンクを持つ項目が表示される問題を修正 (http/https以外のリンクを持つ項目を除外)
- Fix: 引用元のノートが削除されていると、詳細表示で引用枠ごと消えてしまう問題を修正 (削除済みの表示を出すように)
- Fix: 投稿フォームで、宛先に指定済みのローカルユーザーを `@user@自ホスト` と書くと未指定として警告される問題を修正
- Fix: QRコードリーダーで、カメラの起動完了を待つ間にページを離れると起動中の表示のまま残る問題を修正
- Fix: ウェルカム画面 (classic / simple) で、背景装飾の上ではマウスホイールでスクロールできない問題を修正
- Fix: グリッド・省略表示テキスト・通知タイムラインが、閉じた後もリサイズ監視やスクロール監視を残し続ける問題を修正
- Fix: ドライブ・ギャラリー・誕生日ウィジェットから内容が入った状態で開く投稿フォームが、書きかけの下書きを上書き・復元してしまう問題を修正
- Fix: blurhashのプレースホルダーが元画像の縦横比ではなく読み込み中の実サイズで描画される問題を修正
- Feat: アップロード待ちのファイルを、サムネイルのクリックまたは詳細メニューの「プレビュー」からビューアーで確認できるように (動画も対象)
- Fix: `misskey-js` のモデレーションログ型に `pauseQueue` / `resumeQueue` が含まれておらず、キューの一時停止・再開のログを型付きで扱えなかった問題を修正

### Server
- Fix: ページングの `untilDate` / `sinceDate` に `0` (UNIXエポック) を渡すと境界が無視されていた問題を修正 (`untilDate: 0` はエポック以前を求める指定だが、全件が返っていた)
- Feat: 読み取りAPI 183本を HTTP QUERY (RFC 10008) でも受け付けるように (POST も従来どおり受ける。QUERY は safe かつ idempotent なので Cache-Control が中間キャッシュに実際に効く)
- Enhance: Hono を 4.13.2 に更新

- Enhance: ジョブキューの BullMQ を v6 に更新 (ioredis 固定ではなくなり、node-redis / Bun 組み込みクライアント / PostgreSQL バックエンドが選択肢に入った)
- Enhance: 配送時のインスタンス参照を組み立て済みクエリの使い回しに変更し、AP配送のCPUを削減 (同一ホストへの大量配送で発行クエリの約8割を占めていた。実測で1配送あたり −10%)
- Fix: 依存を新規に導入した環境でサーバーが `Cannot find native binding` で起動できない問題を修正 (ActivityPubのLD署名で使う `slacc` をバンドル対象から外し、napi-rsのネイティブバイナリをパッケージ自身の位置から解決できるように)
- Enhance: ノート作成時に積む作成後処理ジョブを1回の一括INSERTにまとめ、1投稿あたりのDB往復を削減 (実測56.2→50.2クエリ)
- Enhance: ノート作成の作成後処理から、ステージ毎のロック取得とトランザクションを廃止 (自プロセスが直前に書いてリースを保持している行の所有権確認は自明なため、leaseToken付きの一括DELETEに統合。1投稿あたり実測でクエリ54.1→27.9・CPU −24%・p50 −22%・p95 中央値 −59%。再試行の粒度はステージ毎のまま)
- Enhance: ActivityPubのLD署名で、投稿ごとに秘密鍵をPEMから解析し直していたのをやめ、HTTP署名側と同じ署名器キャッシュを共有 (負荷時の実測でCPU −7%)
- Enhance: LD署名の署名オプションの正規化を、固定構造向けの直接生成に置き換え (jsonld.normalize 比で 91.2% 削減。リレー設定済みインスタンスの投稿経路に効く)
- Enhance: ノート本文のMFMパース結果を使い回すようにし、1投稿につき2回行っていたパースを1回に削減
- Enhance: 同一リクエスト内で3回引いていたロール定義と割り当てを1回に集約 (users/show で role系クエリ 6→3本、notes/create で 6→2本)
- Change: 外部へ送信する User-Agent の製品名を `Misskey` から `Erebia` へ変更 (メディアプロキシの再帰検出は `erebia/` と `misskey/` の両方を弾く)
- Enhance: ノート作成APIの応答を作成後処理の完了を待たずに返すようにし、レイテンシを改善 (負荷下p50 -60%。作成後処理はfanout完了後にバックグラウンドで逐次実行され、失敗時は従来どおりoutbox経由で再実行される)
- Enhance: ランタイムをBun 1.4.0へ更新し、Node.js / pnpm ベースの実行環境要件を廃止 (Docker Image も `oven/bun:1.4.0` 系に更新)
- Fix: AP配送の失敗時に行うインスタンス情報のバックグラウンド更新が、DB障害時に未処理エラーになり得る問題を修正 (エラーをログへ記録するように)
- Fix: ノート作成・ユーザー停止・ドライブ削除の副作用を永続化し、Webhook・通知・上限判定の競合を修正
- Fix: アンテナの受信ソース「フォローしているユーザーのノート」が判定処理を持たず、実質「全てのノート」と同じ挙動になっていた問題を修正 (自分の投稿とフォロー中ユーザーの投稿のみにマッチするように)
- Fix: リストを指定しないリストアンテナを作成・更新できないように (作成できても何にもマッチしないため。DB側にもCHECK制約を追加)
- Fix: アンテナのインポートで不正なファイルを1回送っただけで1時間分の実行枠を失う問題を修正 (ファイル検証を通過してから枠を消費するように変更)
- Fix: `admin/queue/queue-stats` のレスポンス型宣言が実際に返すoutbox統計と食い違っていた問題を修正
- Fix: `admin/queue/outbox-dead-letters` の `data` / `opts` をオブジェクトと型宣言していた問題を修正 (オブジェクトでない値こそが隔離理由になるため、実際の値と生成SDKの型が食い違っていた)
- Fix: Fanoutタイムラインから `users/notes` を返す際、センシティブチャンネルの投稿が他人にも表示される問題を修正 (ノートのチャンネル情報が読み込まれておらず、除外判定が常に素通りしていた)
- Fix: `users/notes` でセンシティブチャンネルの投稿がリノート経由で他人にも表示される問題を修正 (リノートは自身のチャンネルを持たないため、`withChannelNotes` を指定しない場合でも本文ごと露出していた)
- Fix: ロールのポリシーが不正な形式で保存されていると、そのロールを持つユーザーの全APIが500になる問題を修正 (作成・更新時に形式を検証し、既存の壊れた値は既定値へフォールバック。数値ポリシーがNaNになり上限判定が素通りする問題も解消)
- Fix: オブジェクトストレージへのアップロードに失敗しても成功として扱われ、実体の無いファイルがドライブに登録される問題を修正 (リクエストをエラーで終わらせ、部分的に書き込まれたオブジェクトを削除するように)
- Fix: 壊れた形式のValkey pub/subメッセージでストリーミングのプロセスが停止する問題を修正 (`channel` が文字列でない・`error` である場合を含め、不正なpayloadは黙って無視するように)
- Fix: WebSocket接続の初期化中に切断された場合、閉じたソケットに対する接続が残り続ける問題を修正
- Enhance: 高頻度なDBクエリのSQL組み立てを1回だけ行って使い回すように (drizzleは実行のたびにSQL文字列を組み立て直しており、ホームタイムライン1リクエストあたりのCPUの約40%を占めていた。ノート・アンケート・チャンネル・ドライブファイルの取得にも適用し、同時128接続の負荷計測で `/api/notes` の rps +23〜29%、リクエストあたりCPU −23%、p50 −26%。PostgreSQL側は従来どおり無名文のまま=プランは毎回作り直すため、汎用プランによる劣化は起きない)
- Enhance: タイムライン取得時のフォロー・ミュート・ブロックの問い合わせを1本にまとめるように (SQL1本あたりの固定CPUがDBクライアント処理の大半を占めていたため、往復を減らした。`users/notes` と `notes/timeline` のp50が約25%、`notes/hybrid-timeline` が約20%短縮)
- Fix: チャートのhour/day更新が別トランザクションに分かれており、片方だけ失敗すると次回保存で成功済みの期間へ同じ差分が二重加算される問題を修正
- Fix: outboxに未処理の行が1件でもあると `admin/queue/queues` と `admin/queue/queue-stats` が必ず500になる問題を修正
- Fix: APIの予期しない例外がサーバーログに一切残らず、500の原因を追跡できない問題を修正 (エラーIDを添えて記録するように)
- Fix: パスワード誤入力・2FA失敗・パスワードリセットリンクの期限切れ・存在しないユーザー指定が500 (`INTERNAL_ERROR`) として返る問題を修正 (`i/change-password` / `i/regenerate-token` / `i/delete-account` / `i/update-email` / `reset-password` / `federation/update-remote-user` に原因が分かるAPIエラーを定義)
- Fix: ActivityPub Delete配送を待つ必要が無いアカウント削除で、ジョブ発行がoutboxのポーリング周期ぶん遅れる問題を修正 (即時発行した行を確実に削除し、ジョブ完了後にディスパッチャが同じジョブを作り直して削除処理が二重実行される問題も防止)
- Enhance: Queue outboxの配送突合を1ジョブ1往復のBullMQ問い合わせから一括pipelineへ変更し、1周あたりの処理件数を100→500件に拡大 (500件の突合でValkeyコマンド 3,500→1,500回、ドレインに要するpoll 5周→1周)
- Fix: アンテナのインポートを上限判定と一括作成を含む単一トランザクションへ統合し、並行作成による上限超過と部分インポートを防止 (あわせてDB書き込みを件数比例の逐次処理から1回の一括INSERTへ変更し、不要なDBキュー経路を削除)
- Fix: ジョブキューの待機中にValkeyコマンドタイムアウトが繰り返し発生する問題を修正
- Fix: 分散レート制限の時刻・リクエスト識別・設定検証と、IP制限無効時のユーザー単位制限を修正
- Fix: ローカルアカウント削除時のActivityPub Delete配送を永続化し、配送完了前の署名鍵削除を防止
- Fix: ActivityPub Delete配送の最終失敗・不正payloadをdead letterとして保持し、管理者のretryまたは明示放棄までアカウント物理削除を停止
- Enhance: Queue outboxの状態・lease・coordinatorを正規化し、BullMQ操作をDB row lock外へ分離して配送確認pollを指数backoff化
- Fix: 非配信広告一覧のカーソル絞り込みを修正
- Fix: ユーザーリスト・クリップのお気に入り登録競合時のAPIエラーを修正
- Fix: ActivityPubのカスタム絵文字・リアクション(Like)・フォロー関係のオブジェクトURIがHTMLを返しており、リモートから解決できなくなっていた問題を修正
- Fix: `mute/create` / `renote-mute/create` / `admin/system-webhook/test` にレートリミットが適用されていなかった問題を修正
- Fix: `admin/roles/users` のAPI定義が認証不要と誤って宣言されていた問題を修正 (実装は認証必須のまま)
- Fix: `hashtags/trend` のPOSTにキャッシュヘッダが付いていなかった問題を修正
- Enhance: 言語コード表 (langmap) がbackendとfrontendに完全重複していたのをmisskey-jsへ集約
- Fix: チャンネルとお知らせの個別ページでサーバーサイドのOGPメタデータが出力されず、リンク共有時にカード表示にならない問題を修正
- Fix: 埋め込みページ (`/embed/*`) が配信されず、`X-Frame-Options: DENY` により外部サイトのiframeに埋め込めなくなっていた問題を修正
- Fix: 埋め込みページの可視性判定を通常ページと同一basisに統一 (非公開クリップ・凍結ユーザー・`requireSigninToViewContents` 設定・`ugcVisibilityForVisitor` 設定が埋め込み経由で迂回されないように)
- Fix: サーバー内部エラー時の応答がプレーンテキストになっており、問い合わせ用のエラーIDが表示されない問題を修正
- Enhance: サーバー設定を厳密な `configVersion: 2` の階層構造へ再設計し、DB・Valkey・HTTP・キュー・ログを詳細設定できるように
- Enhance: URLプレビューの結果をプロセスメモリへキャッシュし、同一URLの重複取得を抑制
- Enhance: OpenTelemetryの外向きtrace伝播先とinstrumentation無効化を個別設定可能にし、既定の外部伝播を停止
- Fix: OpenAPIのエラーstatus・schema・exampleと実際のAPI応答を一致させ、複数endpointの誤った500応答を修正
- Fix: staleなroot情報により管理用アカウント作成の認可を回避できる問題を修正
- Fix: 複数ワーカー環境のOAuth認可、投稿後処理、Renote取り消し、Page/Playのいいね件数、予約投稿キュー、分散ロックの整合性を改善
- Enhance: アンテナ判定のDB取得と同時実行数を抑制
- Fix: 指定公開ノートの宛先数制限、OAuth認可コード再利用時のトークン失効、おすすめユーザーの除外条件を修正
- Enhance: Dockerイメージを軽量化し、Composeのログ管理・ヘルスチェック・正常終了処理を改善
- Enhance: 固定長APIリクエストの本文解析を高速化
- Enhance: HTTPリクエスト処理、OpenAPIの遅延読み込み、起動時の設定ロードを最適化
- Enhance: Fanout Timelineのノート取得とreply・renoteのpack処理を一括化
- Enhance: Fanout TimelineのRedisリスト結合時の重複した並べ替えを削減
- Enhance: ユーザー数が多い環境でのユーザー検索を最適化
- Enhance: 管理画面で全ユーザーを作成日時順に深くページングする際のDB読み取りを削減
- Enhance: single-process構成でserverとqueueのDB・Valkey接続とruntime servicesを共有
- Enhance: ユーザー一覧の関係状態取得を対象ユーザーだけに絞り、大規模アカウントでのDB転送量を削減
- Enhance: チャット履歴の相手・ルームごとの最新メッセージ取得を単一DBクエリへ集約
- Enhance: ActivityPubのフォロー・フォロワー一覧でユーザー取得を一括化
- Enhance: チャットメッセージの参加者・リアクションユーザー取得を一括化
- Fix: 鍵アカウント解除時のフォローリクエスト一括承認で同時実行数を制限
- Fix: DBから取得するタイムラインとノート検索で、CW・返信・添付・アンケートを含む引用ノートが純粋なリノートとして除外される問題を修正
- Fix: アンケート投票を直列化・トランザクション化し、並行投票と集計不整合を防止
- Fix: migrationの同時実行、履歴改変、リリース・tarball・Compose設定の安全性を改善
- Enhance: キャッシュ・ジョブキュー基盤をRedisからValkey 8へ移行
- Enhance: ジョブキューのバッチ処理、再試行、並列度、永続outboxを最適化
- Fix: プロキシ経由のIP判定、パスワードリセット、ActivityPub署名、内部エラー応答などの安全性と整合性を改善
- Fix: 予約投稿とリアクション更新をPostgreSQLトランザクションで原子的に処理し、プロセス停止時の投稿重複・集計欠損を防止
- Enhance: アンテナのユーザー指定で `*@example.com` によるサーバー全体の指定に対応
- Enhance: 新規登録にIP単位の最短間隔・時間あたり件数制限を追加
- Enhance: 通知を本人のタイムラインから個別に削除する `notifications/delete` APIを追加
- Enhance: `notes/search` APIに添付ファイル、センシティブファイル、返信、引用、CW、公開範囲のフィルターを追加
- Enhance: 旧音声 MIME エイリアス (`audio/x-flac` / `audio/vnd.wave`)、OStatus の WebFinger subscribe リンク、`channels/timeline` の未使用 `allowPartial` パラメータを削除
- Enhance: `notes/reactions`/`users/reactions` と `i/favorites`、通知、ノート下書き/Pages/Gallery/Play/Clips/Chat/ブロック/ミュート/ロール/ユーザー詳細/ユーザーリスト/管理ユーザー一覧の pack 処理をバッチ化し、一覧取得時のDBアクセスを削減
- Enhance: ドライブファイルの詳細 pack と管理ドライブのファイル一覧でフォルダ詳細の pack 処理をバッチ化し、フォルダ数・ファイル数集計のDBアクセスを削減
- Enhance: 絵文字の一括更新通知で更新後の絵文字取得を一括化し、管理操作時のDBアクセスを削減
- Fix: カスタム絵文字の一括更新・削除をトランザクション化し、部分適用を防ぎつつDB書き込みを一括化
- Fix: 公開ユーザーリストの複製をトランザクション化し、メンバー検証とDB書き込みを一括化
- Enhance: お知らせ一覧の既読状態取得をバッチ化し、ログイン中の一覧取得時のDBアクセスを削減
- Enhance: `my/apps` の認可状態取得をバッチ化し、アプリ一覧取得時のDBアクセスを削減
- Enhance: アカウント移行時のブロック/ミュート/ロール/ユーザーリスト引き継ぎ処理を効率化し、大量の関係データを持つアカウントの移行時のCPUコストと不要なDBアクセスを削減
- Enhance: 複数の通報を system webhook へ通知する際のユーザー情報 pack を一括化し、通知時のDBアクセスを削減
- Enhance: ホーム/ソーシャルタイムライン取得時のチャンネルミュート判定を効率化
- Enhance: ActivityPub Note 生成時の添付ファイル順序復元とカスタム絵文字取得を効率化
- Enhance: ノート差分取得時のリアクションバッファ取得と絵文字解決を効率化
- Enhance: フォロー通知作成時のミュート判定、フォローリクエスト一覧のユーザー pack、一括承認時のユーザー/profile取得、move後の自動承認/インポート判定を効率化
- Enhance: ドライブ容量超過時の古いファイル削除準備で対象ファイル取得を一括化
- Enhance: ユーザー詳細一覧の `alsoKnownAs` 解決を一括化し、remote alias を持つユーザーを含む一覧取得時のDBアクセスを削減
- Enhance: ユーザー・ノート・Webhook の pack 時のカスタム絵文字解決でキャッシュ未命中分を一括取得し、絵文字数に比例するDBアクセスを削減
- Enhance: ノート作成時の mention 解決・hashtag 記録/ランキング・fanout・通知受信判定・通知用ノートpackを一括化し、多数フォロワーへの通知保存を上限付き Redis pipeline 化
- Enhance: ノート・フォロー・ブロックイベントの UserWebhook 取得を対象ユーザーで絞り込み、不要な全件取得を削減
- Enhance: レジストリスコープ一覧、Play一覧のliked判定、ページ更新、絵文字エイリアス一括削除、アバターデコレーション更新時の配列探索を効率化
- Enhance: `pinned-users`、単一ページの添付ファイル取得、ブロック時のユーザーリスト整理、招待コード一括作成のDBアクセスを一括化
- Enhance: `admin/meta` の廃止予定だった `summalyProxy` フィールド (および `admin/update-meta` の同名パラメータ) を削除。`urlPreviewSummaryProxyUrl` を使用すること
- Enhance: ユーザー一覧系エンドポイント (フォロワー/フォロー/ユーザー検索等) を高速化 (1ユーザー毎に発行していたロール・アサイン・ピン留めのクエリをリスト単位の3クエリに集約。負荷計測でp50 122→78ms、DBラウンドトリップ 68k→20k回/3分)
- Enhance: 全文検索・ユーザー検索を pg_trgm でインデックス化 (ノート本文の LIKE フォールバック・ユーザー名/自己紹介の中間一致が行数線形の全表走査だったのを GIN trgm 4本でビットマップスキャン化。検索クエリ mean 4.26→0.55ms)
- Enhance: ノート投稿のレイテンシスパイクを解消 (note テーブルの GIN インデックス4本の fastupdate pending list フラッシュが原因で、投稿の一部が1秒超かかることがあった。`fastupdate = off` に変更し、51万ノート環境の負荷計測で INSERT 最大 1,427ms→8ms を確認)
- Enhance: 1秒を超えたHTTPリクエストをエンドポイント・所要時間つきで警告ログに出力するように (チューニング対象の発見用)
- Fix: ファイルアップロードのサイズ超過判定を「全体をメモリに読み切った後」から「読みながら打ち切り」に変更 (メモリ保護の復元)。あわせて Bun ランタイムで maxFileSize が 128MiB を超える設定でもアップロードできるように `maxRequestBodySize` を設定
- Fix: ActivityPub のコンテンツネゴシエーションが Accept ヘッダの q 値・優先順位を無視していた問題を修正 (upstream の `accepts` ライブラリ相当の RFC 7231 準拠ネゴシエータを内製。`q=0` の明示拒否や `text/html, application/activity+json` のような複合ヘッダで upstream と逆の表現を返すことがあった)
- Enhance: HTTPルーティングを高速化 (2ルートのパターン形状が原因で Hono の SmartRouter が全500超ルートを TrieRouter にフォールバックさせていたのを解消し、RegExpRouter (1回の正規表現マッチ) で動作するように。ルーティング処理が約2.2倍高速化)
- Enhance: API 以外のルート (Web/ファイル/OAuth等) の未捕捉例外をサーバーログに記録するように (従来は Hono デフォルトのログ無し 500)
- Enhance: Node ランタイムの HTTP レスポンス書き込みに backpressure 対応を追加 (遅いクライアントへの大きなレスポンスで書き込みバッファが無制限に膨らむのを防止)
- Enhance: 未メンテの依存パッケージ2個を内製実装で置き換え (`xev` → クラスタ横断イベントバス、`http-link-header` → OAuth 用 Link ヘッダパーサ)
- Enhance: タイムラインのDBフォールバッククエリを軽量化 (全タイムラインで note テーブルへの冗長なセルフJOINを削除し非正規化カラム参照に統一、投票有無チェックを `COUNT(*)` から `EXISTS` に変更、フォロー先IDリストを数万個のプレースホルダ展開から配列1パラメータに変更)
- Enhance: 連合チャートの集計クエリを改善 (`COUNT(DISTINCT ...)` による following 全表ソートを recursive CTE のインデックス走査に変更)
- Fix: ブロック済みホストを設定しているサーバーで連合チャートの集計 (sub/pub/pubsub) が SQL エラーで毎回失敗していた問題を修正 (配列パラメータの渡し方の誤り)
- Enhance: ユーザー名の前方一致検索用インデックス (`varchar_pattern_ops`) を追加し、ユーザー名補完・検索の全表走査を解消
- Enhance: ActivityPub 配送のCPU負荷を削減 (署名鍵のパース結果をキャッシュしジョブ毎の再パースを回避、鍵取得のDBアクセスをキャッシュ化、配送停止インスタンス判定を配列走査からSet参照に変更)
- Enhance: ワードミュート等の正規表現マッチングでコンパイル済みインスタンスを使い回すように変更 (ノート×ユーザー数分のRE2再コンパイルを回避)
- Enhance: `following`/`note_reaction` テーブルにページング用の複合インデックスを追加し、フォロワー一覧・リアクション一覧の大規模データでの応答を改善
- Enhance: RSSフィード生成・ActivityPub受信ノートの添付ファイル取得を一括化/並列化
- Enhance: タイムライン等の複数ノート取得時のデータベース/Redisアクセスを一括化 (ノート毎に個別発行していた投稿者・添付ファイル・チャンネル・投票データ・カスタム絵文字・followers公開判定・自分のリアクション・リアクションバッファの取得をリクエスト単位でバッチ化。20件のタイムラインで最大100クエリ超→数クエリに削減)
- Enhance: パスワードのハッシュ化/照合を Bun ランタイムではネイティブ実装 (`Bun.password`) で行うように変更しサインイン/サインアップを高速化 (既存の bcrypt ハッシュとは互換で、Node 実行時や72バイト超のパスワードは従来どおり bcryptjs で処理)
- Enhance: データベース接続を Bun ランタイムではネイティブ実装 (`Bun.sql`) で行うように変更 (実エンドポイントの負荷計測でp50 −5〜−18% / rps +4〜+23%。JS配列・`Date`・結果の形・エラーのSQLSTATEが node-postgres と非互換なのでドライバ境界で吸収している。Node 実行時は従来どおり node-postgres で、`MK_DB_DRIVER=pg` を指定すれば Bun でも戻せる)
- Enhance: バックエンドの依存パッケージを整理し、未メンテのライブラリ16個を削除 (`got`/`fluent-ffmpeg`/`deep-email-validator`/`ratelimiter`/`probe-image-size`/`tinycolor2`/`stringz`/`tmp` 等を fetch・ffmpeg 直接起動・自前実装・`sharp`・`Intl.Segmenter` 等へ置き換え。レート制限は従来と同一の Redis キー形式・アルゴリズムを維持)
- Change: アバター未設定ユーザーの identicon の模様が変わるように (乱数生成器の変更のため。配色パレット等は不変)
- Enhance: 依存パッケージのセキュリティ更新 (間接依存の undici を 7.28.0 に統一し High 3件の脆弱性を解消、AWS SDK 等のマイナー更新、未使用の devDependencies 4件を削除)
- Fix: アカウント移行時に、移行元ユーザーを対象に含むアンテナへ移行先アカウントが引き継がれない問題を修正
- Enhance: ノート作成時のデータベースアクセスを削減 (ハッシュタグ記録の並列化、リノートミュート・スレッドミュート判定の一括クエリ化、ロールポリシー解決の重複排除) し、リレー一覧にインデックスを追加
- Enhance: WebSocket ストリーミングのミュート/ブロック関係を内部イベントで即時更新し、接続数に比例する定期DBポーリングを削除
- Fix: `endpoint` エンドポイントが返すパラメータ型情報で、nullable なパラメータの型ラベルが常に `String` にフォールバックしていた問題を修正
- Enhance: データベースインデックスを追加し、一部のサーバー処理のパフォーマンスと重複登録耐性を改善
- Enhance: バックエンドの重複コード (フォロー/ブロック処理の連合インスタンスキャッシュ処理、ユーザー一括取得のフォールバック処理、各種一覧系エンドポイントのページネーション解決処理) を共通化し、未使用の依存パッケージ (`@nestjs/platform-express`, `accepts`, `vary`) を削除
- Enhance: 投稿の pack 処理でリプライ/リノート先が既に取得済みの場合は再取得しないように改善し、アクセストークンの最終利用日時更新を非同期化
- Enhance: 非公開の Pages / Play のタイトル等メタ情報が未ログイン閲覧者向けページ HTML に含まれないように (公開のもののみ SSR する)
- Fix: ドライブファイルの accessKey が欠損しているレコードを削除する際に、リクエスト全体が 500 エラーになる問題を修正
- Fix: ジョブキューワーカーにストリーム配信機能が配線されておらず、ActivityPub 経由で受信したノート・通知・アンテナ命中等のイベントが WebSocket ストリーミングに一切流れなかった問題を修正
- Fix: ロールポリシーの `rateLimitFactor` (レートリミット緩和・無効化) が API のレート制限判定で無視されていた問題を修正
- Fix: ノート作成時にロールタイムライン (`roles/notes`, `roleTimeline` チャンネル) への配信が行われていなかった問題を修正
- Fix: ノート関連の通知 (リプライ・リノート・引用・メンション・リアクション) の WebSocket 配信が pack 前の内部形式で送られており、クライアントが通知を表示できなかった問題を修正
- Fix: `requiredRolePolicy` 相当のロールポリシー検査 (カスタム絵文字管理・アバターデコレーション管理・チャンネル作成・各種インポート・招待コード) で root ユーザーのバイパスが行われず、root が権限エラーで拒否されていた問題を修正
- Fix: API ハンドラ内の予期しない例外が元実装の `INTERNAL_ERROR` 形式 (`info.e` 付き) でレスポンスされていなかった問題を修正
- Fix: ユーザーの pack 結果で `emojis` (名前・プロフィール等のカスタム絵文字URLマップ) が常に空になっていた問題を修正
- Fix: ActivityPub の `Update(Person)` 受信時に埋め込みオブジェクトを使わず再フェッチしていたため、中間キャッシュ経由で古いプロフィールを取得し更新が反映されないことがある問題を修正
- Fix: `i/move` の移動先・`i/update` の `alsoKnownAs` で未知のリモートユーザーを WebFinger で解決できなかった問題を修正
- Enhance: REST API のリクエストパラメータ検証を `ajv` (JSON Schema) から `Zod` に全面移行し、本番依存から `ajv` を除去。あわせて `users/show`/`notes/create` の OpenAPI 仕様生成 (misskey-js 型生成) を Zod スキーマ由来に統一し、これまで独立して二重管理されていたパラメータ定義の重複を解消
- Feat: ID生成方式を `uuidv7` (RFC 9562、ハイフン無しhex表現) 固定にし、設定ファイルの `id` 項目 (aid/aidx/meid/meidg/objectid/ulid の選択) を削除
- Enhance: 連合インスタンスのメタデータ更新時に旧形式のRedisロックキーを毎回削除していた移行処理を除去
- Fix: ActivityPub Actorにアカウント作成日時が含まれない問題を修正
- Fix: 同じテキストの選択肢を複数持つアンケートで、票数が最初の同名選択肢のものに揃ってしまう問題を修正 (ActivityPub経由の票数更新とAPI応答の両方で、選択肢テキストの一致ではなく並び順で対応付けるように)
- Fix: ブロックによる強制フォロー解除の `unfollow` ストリーミングイベント・Webhookに含まれるユーザー情報から、閲覧者視点の関係フィールド (`isFollowing` 等) が欠落していた問題を修正 (通常のフォロー解除経路と実装を共通化)
- Fix: アカウント移行後の引き継ぎ処理 (ブロック/ミュート/ロール/リスト/フォロー数調整) で1つでも失敗すると残りが実行されず、しかもエラーがログに一切残らない問題を修正 (各処理を独立して完走させ、失敗した処理名を記録するように)
- Enhance: 読み取り側が存在しないRedisキャッシュへの書き込みを削除 (フォロー・ブロック・ミュート・リノートミュート・プッシュ購読・カスタム絵文字・連合インスタンスの更新のたびに、全件をDBから取得してJSON化しRedisへ書き込んでいたが、参照する経路は既に無かった)
- Fix: `queue-stats` / `server-stats` デーモンがHTTPプロセスの数だけ重複起動し、同じ統計が多重にストリーミング配信されていた問題を修正 (ホスト全体で1プロセスだけが持つように。ワーカー3プロセス構成でValkey接続 51→47)
- Fix: 死んだワーカープロセスを再起動する際に役割 (HTTP / キュー / デーモン担当) が失われ、常にHTTPワーカーとして復帰していた問題を修正
- Enhance: サーバー設定の `server.process.workers` を `httpWorkers` / `queueWorkers` に分割し、HTTPを捌くプロセス数とジョブキューを捌くプロセス数を個別に指定できるように (キュー専用ホスト・HTTP専用ホストの構成が設定だけで組めるように)
- Fix: DB接続プールの上限がプロセス単位で、ワーカーを増やすとPostgreSQLの `max_connections` を超えて接続が頭打ちになる問題を修正 (設定を **ホスト全体の予算** を表す `database.pool.maximumConnectionsPerHost` (既定60) に変更し、DBを使うプロセス数で分割するように。既定構成では1プロセスあたり30接続となり、投稿集中時にプール待ちで発生していた最大3秒超のtailも解消)
- Fix: フォロワー限定・ダイレクトのノートへの返信が、より広い公開範囲のまま投稿できてしまう問題を修正 (フォロワー限定投稿にぶら下がったスレッドがプロフィールやローカルタイムラインから第三者に見えていた)
- Fix: パスワードリセット・MFA解除の対象が管理者の場合、本人以外は一律に拒否するように (rootだけでなく、管理者どうしのアカウント乗っ取りも防ぐ。エラーは `CANNOT_RESET_PASSWORD_OF_ROOT_USER` から `ACCESS_DENIED` に変更)
- Fix: `admin/accounts/create` をrootしか使えず、後から管理者ロールを付与したアカウントが弾かれていた問題を修正 (管理者なら使えるように)
- Fix: `admin/queue/stats` が必要とするAPI権限が `read:admin:emoji` になっていた問題を修正 (`read:admin:queue` に訂正)
- Fix: 既にフォロー済みのチャンネルを再度フォローする / 既にミュート済みのスレッドを再度ミュートすると500になる問題を修正 (`ALREADY_FOLLOWING` / `ALREADY_MUTING` を返すように)
- Fix: ドライブへのアップロードで一時ファイルの書き出しに失敗すると、そのファイルがディスクに残り続ける問題を修正
- Enhance: 未認証で叩けるRSS取得を強化 (IPレート制限とレスポンスサイズ上限、スキーマとURL長の検証、認証情報入りURLの拒否、同一URLへの同時取得の共有、同時実行数の上限、XMLパースの非同期化、取得先の詳細を含まないエラー応答)
- Enhance: センシティブメディアの判定を外部サービス ([sensitive-detector](https://github.com/misskey-dev/sensitive-detector)) に分離し、`nsfwjs` / `@tensorflow/tfjs(-node)` の同梱と NSFW 判定モデルを廃止
- Fix: `/stats` API のレスポンス型が正しくない問題を修正
